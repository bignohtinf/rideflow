# Bản DEMO - phiên bản được PIN để tái lập (reproducibility).
# Nhẹ hơn requirements.txt production: không cần Spark/Kafka/Redshift/Redis.
scikit-learn==1.5.2
lightgbm==4.5.0
pandas==2.2.3
numpy==1.26.4
pyarrow==17.0.0
mlflow==2.17.2
feast==0.40.1
fastapi==0.115.4
uvicorn==0.32.0
httpx==0.27.2
loguru==0.7.2
pyyaml==6.0.2

# Orchestration (Dagster) — ghim <1.9 để giữ protobuf<5 cho feast/mlflow
protobuf==4.25.9
dagster==1.8.13
dagster-webserver==1.8.13

# Monitoring (serve expose /metrics cho Prometheus scrape)
prometheus-client==0.25.0
